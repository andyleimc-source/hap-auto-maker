"""雷达图 — 已合并入 table.py（共用 reportType=8, pivotTable 结构）。"""

from __future__ import annotations

CHARTS = {}


def build(report_type: int, chart: dict, app_id: str) -> dict:
    raise NotImplementedError("雷达图已合并入 table.py (reportType=8)")
