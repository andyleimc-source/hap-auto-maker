"""
增量操作模块 — 对已有应用做局部增删改查。

子模块:
  app_context   — 获取应用完整上下文（工作表 + 字段 + 视图 + 工作流列表）
  add_workflow  — AI 规划并创建新工作流
  add_worksheet — AI 规划并创建新工作表（含字段）
  add_field     — AI 推荐配置并创建新字段
  add_view      — AI 规划并创建新视图
  add_chart     — AI 规划并添加图表到页面
"""
